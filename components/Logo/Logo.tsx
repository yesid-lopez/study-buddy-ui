import { motion } from "framer-motion";
import React from "react";

const Logo = () => {
  return (
    <motion.div className="flex items-center justify-center gap-2 py-2 px-3 rounded-full text-xs">
      SB
    </motion.div>
  );
};

export default Logo;
